{
"name": "django-react",
  "version": "1.0.0",
  "main": "index.js",
  "private": true,
  "scripts": {
    "heroku-prebuild": "NODE_ENV=production cd frontend/ && npm install && npm run build && cd .."
  }
}